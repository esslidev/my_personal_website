user www-data;
worker_processes auto;
pid /run/nginx.pid;
error_log /var/log/nginx/error.log;
include /etc/nginx/modules-enabled/*.conf;

# Minimal events section to satisfy Nginx
events {
    worker_connections 1024;
}

# http context to define HTTP server settings
http {
    # Add MIME types support
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    # Add MIME type for WebAssembly (.wasm)
    types {
        application/wasm wasm;
    }

    # Enable gzip compression
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

    # server block for handling requests
    server {
        listen 80;
        server_name localhost;
        root /usr/share/nginx/html;
        index index.html;

        # Main location block for SPA
        location / {
            try_files $uri $uri/ /index.html =404;
            add_header Cache-Control "no-cache";
        }

        # Handle static assets with caching
        location /assets/ {
            expires 1y;
            add_header Cache-Control "public, no-transform";
        }

        # Handle Flutter-specific files
        location /main.dart.js {
            expires 1y;
            add_header Cache-Control "public, no-transform";
        }

        location /flutter_service_worker.js {
            expires 0d;
            add_header Cache-Control "no-cache";
        }

        # Handle WebAssembly files (.wasm)
        location ~* \.wasm$ {
            add_header Content-Type application/wasm;
            expires 1y;
            add_header Cache-Control "public, no-transform";
        }
    }
}
